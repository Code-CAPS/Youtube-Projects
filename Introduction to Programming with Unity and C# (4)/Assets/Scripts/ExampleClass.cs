using UnityEngine;

public class ExampleClass : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // the 'name' variable below represents the name of the GameObject
        // inheriting from MonoBehaviour allows us to access it
        this.name = "Hello World!";
    }

    // Update is called once per frame
    void Update()
    {

    }
}
